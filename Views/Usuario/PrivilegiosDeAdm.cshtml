@{
    ViewData["Title"] = "Exclusao sucesso";
}

<div class="card text-white text-center bg-primary">
  <div class="card-body">
    <h4 class="card-title">Nescessario Mais privelegios para acessar essa pagina!</h4>
  </div>
</div>